# PRZERÓBKI — rejestr naszych odstępstw od upstreamu

Zasada: KAŻDA zmiana względem upstreamu ma tu wpis (co, po co, status
upstream). Aktualizacja OpenTranscribe = rebase naszych commitów na nowy
release TAG + przejście tabeli od góry + testy + smoke E2E.
Cel długofalowy: tabela pusta (wszystko przyjęte upstream) albo bliska
pustej.

| # | Pliki | Co i po co | Status upstream |
|---|-------|-----------|-----------------|
| 1 | `backend/app/services/asr/elevenlabs_provider.py` (NOWY), `factory.py` (4 wpisy), `schemas/asr_settings.py` (enum), `api/endpoints/asr_settings.py` (2 listy), `frontend/src/lib/api/asrSettings.ts` (typ+etykieta), `tests/unit/test_provider_sdk_compat.py` (1 linia), `tests/unit/test_elevenlabs_provider.py` (NOWY, w tym test parzystości katalog↔walidacja panelu) | Provider ElevenLabs Scribe (nasz silnik docelowy): diaryzacja słowo-po-słowie → segmenty, base_url konfigurowalny (EU residency / atrapa). Uzupełnienie 2026-08-14: provider w walidacji PANELU (enum+listy+frontend) — brak powodował HTTP 422 przy „Test connection"; test parzystości pilnuje kompletu na przyszłość | DO ZGŁOSZENIA jako PR — architektura providerów jest na to otwarta (przewodnik w `asr/CLAUDE.md`) |
| 2 | `backend/app/core/capabilities.py` (`_parse_capability_overrides`), `tests/unit/test_capability_overrides.py` (NOWY) | Env `CAPABILITY_OVERRIDES` = wyłączanie powierzchni funkcji w self-hosted (u nas: `chat.rag=false,chat.ungrounded=false`) — chowa czat z UI i 404-uje router | DO ZGŁOSZENIA jako PR — wpisuje się w ich własny opis „server-driven feature gating"; do czasu przyjęcia utrzymujemy lokalnie |
| 3 | `frontend/vite.config.ts` (5 linii) | Targety dev-proxy z env: `VITE_BACKEND_URL` (fallback `backend:8080`) i `VITE_MINIO_URL` (fallback `minio:9000`, w tym nagłówek Host — podpis presigned URL obejmuje hosta) — dev poza Dockerem | DO ZGŁOSZENIA jako PR (czysto addytywne) |
| 4 | `backend/requirements-lite.txt` — dopisane: cachetools, imohash, nltk (wersje jak w pełnym requirements.txt) | Obraz lite NIE WSTAJE bez nich (`main.py`: No module named cachetools) — potwierdzone na serwerze 2026-08-14 na obrazie z CI, wcześniej maskowane ręczną instalacją w venv środowiska testowego | DO ZGŁOSZENIA jako ISSUE/PR — realny błąd upstreamu w obrazie lite |
| 5 | `frontend/src/lib/i18n/locales/pl.json` (NOWY, 4908 kluczy), `frontend/src/lib/i18n/languages.ts` (1 linia) | Polska wersja UI; parity-check upstreamu przechodzi (`npm run check:i18n`) | DO ZGŁOSZENIA jako PR — czysto addytywny, najlepszy kandydat |
| 6 | `frontend/src/assets/logo-banner.png`, `logo-icon.png` (nadpisane, te same nazwy/wymiary) | Neutralne logo zastępcze („Transkrypcja"/„T") zamiast brandingu OpenTranscribe; docelowo logo firmowe. Informacje licencyjne (About/AGPL) pozostają nietknięte. | LOKALNE — celowo nie do upstreamu |
| 7 | `.github/workflows/build-images.yml` (NOWY), `docker-compose.ghcr.yml` (NOWY), `start-serwer.sh` (NOWY), `instaluj-serwer.sh` (NOWY — instalacja jednym przejściem: zasoby → .env z sekretami → klucz → start), `docker-compose.prod.yml` i `docker-compose.lite.yml` (usunięte linie `pull_policy`) | Warstwa wdrożeniowa forka: CI buduje obrazy lite do ghcr.io (paczki publiczne, serwer ciągnie anonimowo); overlay ghcr podmienia obrazy na nasze; skrypt startowy skleja yml+prod+lite+ghcr. `pull_policy: always` upstreamu WYCIĘTE z prod/lite (2026-08-14): każdy start pytał rejestr i wywracał się na niedomagającym DNS serwera („server misbehaving"); nakładka z `pull_policy: missing`/`if_not_present` odpadła, bo compose na serwerze (Podman 5.4.0) jej nie trawi („pull policy not allowed"); bez tej opcji domyślne zachowanie = obraz lokalny, pull tylko gdy brak. Świadomy koszt: aktualizacja obrazów wymaga jawnego `podman pull`. Overlay ghcr wymusza też obraz lite dla serwisów wyłączanych przez `deploy.replicas=0`/profile GPU (`celery-worker`, `celery-worker-gpu-*`): podman-compose ignoruje replicas i ściągał im PEŁNY backend 8,9 GB (incydent 2026-08-14, dysk 15 GB). | LOKALNE — specyficzne dla naszego wdrożenia; przy rebase na nowy tag ponowić wycięcie |
| 8 | `backend/app/api/endpoints/files/url_processing.py` (dependency na POST `/process-url`), `frontend/src/components/FileUploader.svelte` (zakładka URL za capability), `backend/tests/unit/test_capability_overrides.py` (+1 test pilnujący bramki) | Podłączenie capability `url_ingest`: klucz istniał w rejestrze, ale NIC go nie egzekwowało — `url_ingest=false` w CAPABILITY_OVERRIDES nie robiło nic. Teraz: endpoint zwraca 404 przy wyłączeniu (super_admin ma bypass — mechanizm `require_capability`), zakładka „URL" znika z okna uploadu. U nas: wyłączenie ściągania nagrań z internetu (decyzja usera 2026-08-17) | DO ZGŁOSZENIA jako PR — realna luka upstreamu (klucz zadeklarowany, nieegzekwowany) |
| 9 | `backend/app/api/endpoints/admin.py` (helper `_assert_can_act_on_target` + 3 wywołania), `backend/tests/unit/test_super_admin_protection.py` (NOWY) | Naprawa eskalacji z audytu 2026-08-17: `get_current_admin_user` wpuszcza rolę `admin`, a `/lock`, `/unlock`, `DELETE /sessions` nie sprawdzały hierarchii — zwykły admin mógł wyłączyć i wylogować jedynego `super_admina` (DoS na konto awaryjne). Wzorzec skopiowany z istniejącego `_validate_user_deletion`, który tę kontrolę już miał; test pilnuje, że każdy z 3 endpointów faktycznie woła helper | DO ZGŁOSZENIA jako PR — luka upstreamu, naprawa w ich własnym wzorcu |
| 10 | `backend/app/auth/ldap_auth.py` (`_auto_bind_mode` + 2 wywołania), `backend/tests/unit/test_ldap_starttls.py` (NOWY) | Naprawa z audytu 2026-08-17: `use_tls` (StartTLS) był czytany z .env/DB/UI, ale bind używał `AUTO_BIND_TLS_BEFORE_BIND if cfg.use_ssl` — StartTLS odpalał się TYLKO gdy połączenie i tak było LDAPS, nigdy na porcie 389, gdzie jest jedynym szyfrowaniem. Konfiguracja, do której zachęca panel (SSL off + StartTLS on), wysyłała hasła czystym tekstem. Ścieżka LDAPS bez zmian | DO ZGŁOSZENIA jako PR — poważna luka upstreamu (hasła plaintext przy udokumentowanej opcji) |
| 11 | `backend/app/api/router.py` (bramka `capability="redaction.user"`), `backend/tests/unit/test_capability_overrides.py` (+1 test) | Zasada użytkownika 2026-08-17: funkcje mają być NIEDOSTĘPNE, nie tylko ukryte w UI. `redaction.user` był u upstreamu świadomie „UI-only" — panel znikał, ale `PUT /user-settings/redaction` działał, a przez pole `detectors=['llm']` szła treść transkryptu do zewnętrznego LLM. Teraz router 404-uje. Frontend woła te endpointy tylko z bramkowanego panelu (sprawdzone), więc bez skutków ubocznych | DO ZGŁOSZENIA jako PR — zmienia świadomą klasyfikację upstreamu (Tier 2), więc do dyskusji; u nas zostaje niezależnie od decyzji |
| 12 | `docker-compose.ghcr.yml` (mem_limit/cpus dla 11 serwisów aplikacyjnych) | Limity zasobów z audytu 2026-08-17: upstream nie ustawia żadnych, więc jeden duży plik mógł wyczerpać RAM hosta i OOM-killer ubijał postgresa (awaria dla wszystkich). Sufity dobrane pod 8 CPU/16 GB; infra (postgres/opensearch/minio/redis) celowo bez limitów — limit poniżej heapu OpenSearcha = pewny OOM. `mem_limit`/`cpus` zamiast `deploy.resources` (działa też w podman compose) | LOKALNE — wymiarowanie pod nasz serwer; sam brak limitów wart zgłoszenia jako issue |
| 13 | `backend/app/core/capabilities.py` (`DEPLOYMENT_DISABLED_CAPABILITIES` + 1 linia w resolverze), `backend/tests/unit/test_capability_overrides.py` (3 testy, 2 zaktualizowane), `backend/tests/unit/test_chat_tenant_limits.py` (skipif na 1 teście upstreamu zakładającym włączony chat.ungrounded — 2026-08-18) | Wyłączenia wpisane w OBRAZ, nie tylko w `.env` (zasada usera 2026-08-17 + wniosek z audytu: `.env` na serwerze miał tylko 2 klucze czatu, więc url_ingest/asr.user_providers/llm.user_settings BYŁY WŁĄCZONE mimo decyzji). Baseline: chat.rag, chat.ungrounded, url_ingest, asr.user_providers, llm.user_settings, redaction.user = false. CAPABILITY_OVERRIDES nadal wygrywa (świadome włączenie z powrotem możliwe), ale brak linii w pliku NIE otwiera już funkcji | LOKALNE — to profil naszego wdrożenia; sam mechanizm (poz. 2) jest do PR |
| 14 | `backend/app/schemas/asr_settings.py` (`_validate_base_url_value` + `is_safe_url`), `backend/tests/unit/test_asr_base_url_ssrf.py` (NOWY) | Naprawa SSRF z audytu 2026-08-17: walidator sprawdzał tylko schemat URL, choć docstring twierdził „SSRF protection" — każdy zalogowany mógł wskazać `http://opensearch:9200`/`169.254.169.254` i użyć „Test connection" jako skanera sieci wewnętrznej. Podpięty `url_validation.is_safe_url` (ten sam, którego używa ścieżka media-source): loopback/prywatne/link-local odrzucane. Zweryfikowane: `https://api.elevenlabs.io` przechodzi. UWAGA: atrapa ElevenLabs w środowisku testowym idzie przez `ELEVENLABS_BASE_URL` (factory, bez tego walidatora) — nietknięta | DO ZGŁOSZENIA jako PR — realna luka upstreamu, naprawa ich własnym helperem |
| 15 | `docker-compose.yml` (porty: postgres, redis, minio, opensearch — zakomentowane; backend — bind przez `${BIND_ADDR:-127.0.0.1}`), `docker-compose.prod.yml` (frontend, docs — ten sam bind) | Serwer będzie WSPÓŁDZIELONY z innymi usługami (info usera 2026-08-17), więc bind na 127.0.0.1 przestał wystarczać: każdy proces na hoście mógł czytać OpenSearch (brak jakiegokolwiek uwierzytelniania, a trzyma pełny tekst transkrypcji) i pukać do postgresa/minio/redisa. Host-porty infrastruktury USUNIĘTE — aplikacja i tak łączy się nazwami po sieci compose, przeglądarka pobiera pliki przez nginx frontendu (`/s3/` → `minio:9000`), a diagnostyka idzie przez `docker exec`. Porty aplikacyjne (backend/frontend/docs) domyślnie tylko loopback — tunel SSH działa bez zmian; gdy proxy IT stanie na innej maszynie: `BIND_ADDR=0.0.0.0` w `.env`. Alternatywa (włączenie pluginu bezpieczeństwa OpenSearch) odrzucona jako droższa i ryzykowniejsza: wymaga przekonfigurowania połączeń aplikacji, a nie usuwa ścieżki. UWAGA przy rebase: porty NIE dają się nadpisać nakładką (compose je DOKLEJA — issue #343 upstreamu), stąd edycja plików bazowych | LOKALNE — wymiarowane pod nasze wdrożenie; wart zgłoszenia jako issue: infra bez auth + host-port to zła domyślna para |
| 16 | `backend/app/core/config.py` (`is_lite_deployment`), `backend/app/tasks/transcription/postprocess.py` (flaga `embedding_dispatched` + pominięcie wysyłki w lite), `backend/app/tasks/speaker_embedding_consistency.py` (wczesny return w lite), `backend/tests/unit/test_lite_deployment_guards.py` (NOWY) | Naprawa z pierwszej realnej transkrypcji 2026-08-18: przy cloud ASR + diaryzacji postprocess oddawał ukończenie pipeline'u asynchronicznemu `extract_speaker_embeddings_task` na kolejce GPU — w lite NIKT jej nie czyta, więc plik wisiał na 90% „Processing speaker identification" aż do okresowego recovery. W lite (i przy `CLOUD_ASR_EXTRACT_EMBEDDINGS=false` — wyłącznik ISTNIAŁ w config/.env.example, ale nie był NIGDZIE czytany, ten sam wzorzec co url_ingest) pipeline kończy się od razu w postprocess. Naprawia też upstreamowy bug: nieudany dispatch embeddingu zostawiał plik na 90% na zawsze. Sprawdzacz spójności osadzeń pomija bieg w lite (wysyłał naprawy na martwą kolejkę GPU → wieczne „Repairing 0 of N") | DO ZGŁOSZENIA jako PR — dotyka każdego użytkownika lite+cloud ASR |
| 17 | `backend/app/schemas/media.py` (`_validate_whisper_model_value` — wspólny walidator + odmowa w lite), `backend/app/tasks/transcription/dispatch.py` (`_route_to_cpu_whisper` — chokepoint dla wywołań wewnętrznych), `frontend/src/components/upload/UploadStepModel.svelte` (+prop `allowModelSelection`), `FileUploader.svelte`, `SelectiveReprocessModal.svelte` (ukrycie selecta wg `deployment_mode` ze `/asr-settings/status`), testy w `test_lite_deployment_guards.py` | Dropdown „model Whispera" (upload + reprocess) pozwalał każdemu użytkownikowi wybrać „base" → zadanie szło do `cpu-transcribe` Z POMINIĘCIEM skonfigurowanego silnika chmurowego, a obraz lite nie ma Whispera → gwarantowany błąd. Zasada „niedostępne, nie ukryte": API odrzuca `whisper_model` w lite (422), dispatch ignoruje z logiem (watch sources/batch), UI chowa select | DO ZGŁOSZENIA jako PR — realna pułapka upstreamu w trybie lite |
| 18 | `frontend/src/components/navbar/UserDropdown.svelte` (pozycja menu usunięta), `Navbar.svelte` (import/stan/render AboutModal usunięte) | Decyzja usera 2026-08-18: „O programie" znika też z menu użytkownika (wcześniej poz. z commita 6bfb3a8 przenosiła je tam znad logo). `AboutModal.svelte` zostaje w repo (plik upstreamu, nieużywany); informacje licencyjne AGPL dostępne w repo/stopce docs | LOKALNE — branding naszego wdrożenia |
| — | `.env` (lokalny), `PRZEROBKI.md` (ten plik) | konfiguracja instancji; poza gitem upstreamu | nie dotyczy |

## Procedura aktualizacji (kwartalnie + przy release'ach bezpieczeństwa)
1. `git fetch upstream && git log v<stary>..v<nowy>` — przejrzeć changelog,
   szczególnie `asr/`, `capabilities.py`, `vite.config.ts` (nasze punkty styku).
2. Rebase naszej gałęzi `nasze-dostosowania` na nowy tag.
3. Testy: `pytest tests/unit/test_elevenlabs_provider.py
   test_capability_overrides.py test_provider_sdk_compat.py
   test_capability_contract.py` + pełny zestaw upstreamu.
4. Smoke E2E na atrapie: upload → transkrypcja → TXT z mówcami.
5. Wpis w DECYZJE projektu (wersja, co się zmieniło, czy diff zmalał).
